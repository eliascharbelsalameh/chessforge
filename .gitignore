# personal progress data — never ship this when sharing the project
userdata/

node_modules/
*.log

# built artifact page — regenerate with tools/diagram-desk/build.mjs
tools/diagram-desk/diagram-desk.html
